DROP TABLE IF EXISTS ACCOUNTDETAILS;
CREATE TABLE IF NOT EXISTS ACCOUNTDETAILS (
	ACCOUNTID INT,
	NAME TEXT,
	ISCLIENT BOOLEAN
);

DROP TABLE IF EXISTS PRODUCTS;
CREATE TABLE IF NOT EXISTS PRODUCTS (
	ID INT,
	NAME TEXT,
	Quantity INT,
);
INSERT INTO PRODUCTS VALUES(1,'A',3);
INSERT INTO PRODUCTS VALUES(2,'B',3);
INSERT INTO PRODUCTS VALUES(3,'C',3);

DROP TABLE IF EXISTS TRANSACTIONS;
CREATE TABLE IF NOT EXISTS TRANSACTIONS (
	ID INT,
	BUYERID INT,
	SELLERID INT,
	ProductID INT,
	AMOUNT FLOAT,
);

INSERT INTO ACCOUNTDETAILS VALUES(1, 'Roy', TRUE);
INSERT INTO ACCOUNTDETAILS VALUES(2, 'Dudley', TRUE);
INSERT INTO ACCOUNTDETAILS VALUES(3, 'Sanford', FALSE);
INSERT INTO ACCOUNTDETAILS VALUES(4, 'Lauryn', TRUE);
INSERT INTO ACCOUNTDETAILS VALUES(5, 'Aimee', TRUE);

DROP TABLE IF EXISTS Request;

CREATE TABLE IF NOT EXISTS Request (
account int,
item TEXT,
quantity int);

INSERT INTO Request VALUES (1, 'A',1);
INSERT INTO Request VALUES (2, 'B',1);
INSERT INTO Request VALUES (3, 'C',1);

DROP TABLE IF EXISTS Registered;
CREATE TABLE IF NOT EXISTS Registered (
	account INT,
	address TEXT,
	item TEXT,
	quantity INT,
	time TEXT
);
